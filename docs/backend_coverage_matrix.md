# Backend Coverage Matrix

Backend policy:

- GX: standard single-column expectations (`not_null`, `not_blank`, `regex`, `domain`, `range`, `length`, `type_check`, `uniqueness`, `not_future`).
- Python: composite, conditional, arithmetic comparison, full-row duplicate and dataset-level checks.

Active rule acceptance requires at least one backend, canonical measurement mapping, issue sample behavior and scoring behavior tests.